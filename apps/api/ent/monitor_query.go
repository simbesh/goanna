// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"goanna/apps/api/ent/checkresult"
	"goanna/apps/api/ent/monitor"
	"goanna/apps/api/ent/monitorruntime"
	"goanna/apps/api/ent/notificationevent"
	"goanna/apps/api/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MonitorQuery is the builder for querying Monitor entities.
type MonitorQuery struct {
	config
	ctx                    *QueryContext
	order                  []monitor.OrderOption
	inters                 []Interceptor
	predicates             []predicate.Monitor
	withCheckResults       *CheckResultQuery
	withNotificationEvents *NotificationEventQuery
	withRuntime            *MonitorRuntimeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MonitorQuery builder.
func (_q *MonitorQuery) Where(ps ...predicate.Monitor) *MonitorQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *MonitorQuery) Limit(limit int) *MonitorQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *MonitorQuery) Offset(offset int) *MonitorQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *MonitorQuery) Unique(unique bool) *MonitorQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *MonitorQuery) Order(o ...monitor.OrderOption) *MonitorQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCheckResults chains the current query on the "check_results" edge.
func (_q *MonitorQuery) QueryCheckResults() *CheckResultQuery {
	query := (&CheckResultClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(monitor.Table, monitor.FieldID, selector),
			sqlgraph.To(checkresult.Table, checkresult.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, monitor.CheckResultsTable, monitor.CheckResultsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotificationEvents chains the current query on the "notification_events" edge.
func (_q *MonitorQuery) QueryNotificationEvents() *NotificationEventQuery {
	query := (&NotificationEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(monitor.Table, monitor.FieldID, selector),
			sqlgraph.To(notificationevent.Table, notificationevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, monitor.NotificationEventsTable, monitor.NotificationEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRuntime chains the current query on the "runtime" edge.
func (_q *MonitorQuery) QueryRuntime() *MonitorRuntimeQuery {
	query := (&MonitorRuntimeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(monitor.Table, monitor.FieldID, selector),
			sqlgraph.To(monitorruntime.Table, monitorruntime.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, monitor.RuntimeTable, monitor.RuntimeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Monitor entity from the query.
// Returns a *NotFoundError when no Monitor was found.
func (_q *MonitorQuery) First(ctx context.Context) (*Monitor, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{monitor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *MonitorQuery) FirstX(ctx context.Context) *Monitor {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Monitor ID from the query.
// Returns a *NotFoundError when no Monitor ID was found.
func (_q *MonitorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{monitor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *MonitorQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Monitor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Monitor entity is found.
// Returns a *NotFoundError when no Monitor entities are found.
func (_q *MonitorQuery) Only(ctx context.Context) (*Monitor, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{monitor.Label}
	default:
		return nil, &NotSingularError{monitor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *MonitorQuery) OnlyX(ctx context.Context) *Monitor {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Monitor ID in the query.
// Returns a *NotSingularError when more than one Monitor ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *MonitorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{monitor.Label}
	default:
		err = &NotSingularError{monitor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *MonitorQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Monitors.
func (_q *MonitorQuery) All(ctx context.Context) ([]*Monitor, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Monitor, *MonitorQuery]()
	return withInterceptors[[]*Monitor](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *MonitorQuery) AllX(ctx context.Context) []*Monitor {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Monitor IDs.
func (_q *MonitorQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(monitor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *MonitorQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *MonitorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*MonitorQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *MonitorQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *MonitorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *MonitorQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MonitorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *MonitorQuery) Clone() *MonitorQuery {
	if _q == nil {
		return nil
	}
	return &MonitorQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]monitor.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.Monitor{}, _q.predicates...),
		withCheckResults:       _q.withCheckResults.Clone(),
		withNotificationEvents: _q.withNotificationEvents.Clone(),
		withRuntime:            _q.withRuntime.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCheckResults tells the query-builder to eager-load the nodes that are connected to
// the "check_results" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MonitorQuery) WithCheckResults(opts ...func(*CheckResultQuery)) *MonitorQuery {
	query := (&CheckResultClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCheckResults = query
	return _q
}

// WithNotificationEvents tells the query-builder to eager-load the nodes that are connected to
// the "notification_events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MonitorQuery) WithNotificationEvents(opts ...func(*NotificationEventQuery)) *MonitorQuery {
	query := (&NotificationEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNotificationEvents = query
	return _q
}

// WithRuntime tells the query-builder to eager-load the nodes that are connected to
// the "runtime" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MonitorQuery) WithRuntime(opts ...func(*MonitorRuntimeQuery)) *MonitorQuery {
	query := (&MonitorRuntimeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRuntime = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Label string `json:"label,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Monitor.Query().
//		GroupBy(monitor.FieldLabel).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *MonitorQuery) GroupBy(field string, fields ...string) *MonitorGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MonitorGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = monitor.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Label string `json:"label,omitempty"`
//	}
//
//	client.Monitor.Query().
//		Select(monitor.FieldLabel).
//		Scan(ctx, &v)
func (_q *MonitorQuery) Select(fields ...string) *MonitorSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &MonitorSelect{MonitorQuery: _q}
	sbuild.label = monitor.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MonitorSelect configured with the given aggregations.
func (_q *MonitorQuery) Aggregate(fns ...AggregateFunc) *MonitorSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *MonitorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !monitor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *MonitorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Monitor, error) {
	var (
		nodes       = []*Monitor{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withCheckResults != nil,
			_q.withNotificationEvents != nil,
			_q.withRuntime != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Monitor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Monitor{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCheckResults; query != nil {
		if err := _q.loadCheckResults(ctx, query, nodes,
			func(n *Monitor) { n.Edges.CheckResults = []*CheckResult{} },
			func(n *Monitor, e *CheckResult) { n.Edges.CheckResults = append(n.Edges.CheckResults, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNotificationEvents; query != nil {
		if err := _q.loadNotificationEvents(ctx, query, nodes,
			func(n *Monitor) { n.Edges.NotificationEvents = []*NotificationEvent{} },
			func(n *Monitor, e *NotificationEvent) {
				n.Edges.NotificationEvents = append(n.Edges.NotificationEvents, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withRuntime; query != nil {
		if err := _q.loadRuntime(ctx, query, nodes, nil,
			func(n *Monitor, e *MonitorRuntime) { n.Edges.Runtime = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *MonitorQuery) loadCheckResults(ctx context.Context, query *CheckResultQuery, nodes []*Monitor, init func(*Monitor), assign func(*Monitor, *CheckResult)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Monitor)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CheckResult(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(monitor.CheckResultsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.monitor_check_results
		if fk == nil {
			return fmt.Errorf(`foreign-key "monitor_check_results" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "monitor_check_results" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *MonitorQuery) loadNotificationEvents(ctx context.Context, query *NotificationEventQuery, nodes []*Monitor, init func(*Monitor), assign func(*Monitor, *NotificationEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Monitor)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.NotificationEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(monitor.NotificationEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.monitor_notification_events
		if fk == nil {
			return fmt.Errorf(`foreign-key "monitor_notification_events" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "monitor_notification_events" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *MonitorQuery) loadRuntime(ctx context.Context, query *MonitorRuntimeQuery, nodes []*Monitor, init func(*Monitor), assign func(*Monitor, *MonitorRuntime)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Monitor)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.MonitorRuntime(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(monitor.RuntimeColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.monitor_runtime
		if fk == nil {
			return fmt.Errorf(`foreign-key "monitor_runtime" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "monitor_runtime" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *MonitorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *MonitorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(monitor.Table, monitor.Columns, sqlgraph.NewFieldSpec(monitor.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, monitor.FieldID)
		for i := range fields {
			if fields[i] != monitor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *MonitorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(monitor.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = monitor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MonitorGroupBy is the group-by builder for Monitor entities.
type MonitorGroupBy struct {
	selector
	build *MonitorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *MonitorGroupBy) Aggregate(fns ...AggregateFunc) *MonitorGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *MonitorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MonitorQuery, *MonitorGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *MonitorGroupBy) sqlScan(ctx context.Context, root *MonitorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MonitorSelect is the builder for selecting fields of Monitor entities.
type MonitorSelect struct {
	*MonitorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *MonitorSelect) Aggregate(fns ...AggregateFunc) *MonitorSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *MonitorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MonitorQuery, *MonitorSelect](ctx, _s.MonitorQuery, _s, _s.inters, v)
}

func (_s *MonitorSelect) sqlScan(ctx context.Context, root *MonitorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
